# Implement hash table from scratch

```js
class HashTable {
  // accepts key, value pair
  put(k, v) {}

  //accepts key and return value
  get(k) {
    return v;
  }

  //accepts key and remove corrosponding value
  remove(k) {}

  /*
  constrains 
    1. k : integer
    2. v : string
    3. handle "collision" using "chaining"
   */
}
```
